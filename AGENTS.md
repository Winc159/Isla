# Isla 项目约束

## 当前目标

从一个可在 CLI 中连续完成“用户提问 → 模型回答”的最小 Agent Runtime 开始，逐步验证并扩展，而不是预先实现完整框架。Isla 第一阶段只验证最小闭环，长期目标是成为贴合用户个人习惯的个人 Agent，而不是优先建设通用 Agent 平台。

## 已确认原则

- 主要使用 TypeScript 和 Node.js。
- Runtime 核心采用插件化设计，但第一版只实现验证当前需求所需的最小扩展点。
- 第一阶段支持 OpenAI 与 DeepSeek 云端 API；本地模型以后通过同一模型接口接入。
- 第一阶段只提供 CLI，不开发 Web UI。
- 第一版 CLI 支持进程内连续提问，并保留本次运行中的部分对话上下文；退出后的持久化记忆以后设计。
- 优先保证结构清晰、边界明确、容易维护。
- 避免为尚未出现的问题增加复杂保护逻辑。
- 避免多层包装、多层继承和不必要的嵌套。
- 不因设想中的未来需求提前实现功能；新增抽象必须由至少一个现实需求驱动。
- 外部 Agent 项目可以作为设计参考，但不直接复制成项目架构。
- 参考外部项目时，应明确记录“采用、暂缓、拒绝”的内容及理由；不因参考实现而引入其框架、目录结构或源码。
- 任何实际发送给模型的消息都必须能由 Isla 的会话状态重建。用户输入在调用 Provider 前进入历史；只有有效模型回答才作为 assistant 消息进入历史。
- 项目需要与用户创建的 GitHub 仓库 `https://github.com/Winc159/Isla.git` 同步；未经用户明确要求，不执行提交或推送。
- 项目计划公开开源，并在稳定后通过公共 npm 包分发。
- API Key、访问令牌、真实 `.env`、私人会话内容和其他秘密信息不得进入源码、测试样例、文档、日志、构建产物或 Git 历史。
- 当前 Windows 工作区主要用于代码开发与维护；Isla 后续需兼容 macOS ARM64 与 Linux x64，常驻主机可以是 Mac，也可以是 N100 Linux 主机，暂不把单一设备写死为架构前提。
- 部署方式不得渗透进 Runtime 核心。原生 Node.js 与容器运行应尽量共用同一套核心代码和配置边界。
- 第一版运行期间不动态切换模型或 Provider；启动时确定本次会话使用的 Provider 和模型。
- 本地模型通过 Mac 上运行的局域网 HTTP 模型服务接入。Provider 表示接口协议，主机 IP、端口和模型名称属于连接配置，不把特定 IP 写进 Runtime。
- 正常运行使用 Config/Profile 作为 Provider、模型、Base URL 和 API Key 的配置事实源；不得把 `.env` 当作默认配置来源。`.env` 只作为显式开发、CI 或迁移兼容入口使用，切换 Provider 或模型前先读取 Config/Profile，不要猜测环境变量含义。

## 自动化与验证

- 自动测试属于核心基础设施，不依赖人工逐项验证。
- 项目应逐步形成一组清晰、可直接运行的脚本，覆盖测试验证、构建、发布部署和必要的维护工具。
- 只在相应能力出现后增加脚本，不预先建设尚无使用场景的复杂流水线。

## 文档与知识积累

- 保留文档目录，持续记录架构决策、API 文档链接、使用说明、未来方向和 Bug。
- 文档先保持简单可检索，未来内容增长后再演化为知识库。
- 重大设计变化应记录背景、选择、理由和重新评估条件。

## 工作方式

- 先讨论并确认设计，再开始实现。
- v0 实现以 `docs/architecture-v0.md` 为架构基线，以 `docs/luna-implementation-v0.md` 为执行顺序；如需改变核心契约或扩大范围，先与用户确认并同步文档。
- 每次只加入一个可独立验证的能力，并保持项目始终可运行。
- 设计变化应记录原因，方便未来复盘与研究。
- 未经明确要求，不执行 Git 写操作。

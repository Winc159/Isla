# Isla

Isla v0 is a small TypeScript CLI for continuous in-process conversations with OpenAI, DeepSeek, or an OpenAI-compatible local service.

Copy `.env.example` to `.env`, set `ISLA_PROVIDER`, `ISLA_MODEL`, and the matching API key or `ISLA_BASE_URL`, then run `npm run dev`. Enter `/exit` to quit.

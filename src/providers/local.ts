import OpenAI from "openai";
import type { RuntimePlugin } from "../core/plugin.js";
import type { ModelCallOptions, ModelProvider, ModelRequest, ModelResponse } from "../core/types.js";
import type { LocalConfig } from "../config.js";
import { normalizeProviderError, RuntimeError } from "../core/errors.js";
export function createLocalPlugin(config: LocalConfig): RuntimePlugin { return { name: "local", setup: c => c.registerProvider(new LocalProvider(config)) }; }
class LocalProvider implements ModelProvider { readonly id = "local"; readonly model: string; private readonly client: OpenAI; constructor(config: LocalConfig) { this.model = config.model; this.client = new OpenAI({ apiKey: config.apiKey ?? "isla-local", baseURL: config.baseURL, timeout: config.timeoutMs, maxRetries: 0 }); } async generate(request: ModelRequest, options?: ModelCallOptions): Promise<ModelResponse> { try { const r = await this.client.chat.completions.create({ model: this.model, messages: [...request.messages] as never, stream: false }, options?.signal ? { signal: options.signal } : undefined); const text = r.choices[0]?.message.content; if (!text?.trim()) throw new RuntimeError({ code: "PROVIDER_EMPTY_RESPONSE", recoverable: false, message: "Local 模型返回了空回答。" }); return { text, model: r.model, ...(r.usage ? { usage: { input: r.usage.prompt_tokens, output: r.usage.completion_tokens, total: r.usage.total_tokens } } : {}) }; } catch (error) { throw normalizeProviderError(error, "Local"); } } }
